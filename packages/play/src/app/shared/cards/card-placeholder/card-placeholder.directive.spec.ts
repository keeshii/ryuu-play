import { CardPlaceholderDirective } from './card-placeholder.directive';

describe('CardPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new CardPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
