import { DropHighlightDirective } from './drop-highlight.directive';

describe('DropHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DropHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
